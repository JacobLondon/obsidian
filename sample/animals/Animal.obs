
class Animal {

    // all self.xxxx variables become instances variables
    public fn new: string name {
        // implicitly private, explicitly mutable
        mut string self.name = name;
    }

    fn destroy {
        // deconstructor?
    }
}